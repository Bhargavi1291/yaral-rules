name: UDM Field Check

on:
  pull_request:
    branches:
      - main

jobs:
  check-udm-fields:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Run UDM field check
      uses: ./.github/actions/udm-check
